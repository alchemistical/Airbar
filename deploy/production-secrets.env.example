# AirBar Production Secrets Template
# =====================================
# Copy this file to production-secrets.env and fill in real values
# NEVER commit production-secrets.env to version control

# Database Credentials
POSTGRES_PASSWORD=GENERATE_STRONG_PASSWORD_128_CHARS

# JWT Secrets (Generate with: openssl rand -base64 64)
JWT_SECRET=GENERATE_JWT_SECRET_HERE
REFRESH_SECRET=GENERATE_REFRESH_SECRET_HERE

# Session Security
SESSION_SECRET=GENERATE_SESSION_SECRET_HERE

# Rate Limiting Bypass (For admin/monitoring)
RATE_LIMIT_BYPASS_TOKEN=GENERATE_BYPASS_TOKEN_HERE

# External API Keys
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY
STRIPE_SECRET_KEY=sk_live_YOUR_STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_STRIPE_PUBLISHABLE_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET

# SMTP Configuration
SMTP_HOST=smtp.your-provider.com
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Monitoring & Error Tracking
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project
DATADOG_API_KEY=your-datadog-api-key

# SSL/TLS Configuration
ACME_EMAIL=admin@airbar.com

# Security Notes:
# 1. Use a secrets management system (AWS Secrets Manager, Azure Key Vault, etc.)
# 2. Rotate secrets regularly (JWT secrets every 90 days minimum)
# 3. Use strong, randomly generated passwords (128+ characters)
# 4. Enable database encryption at rest
# 5. Configure proper SSL/TLS certificates via Let's Encrypt or commercial CA